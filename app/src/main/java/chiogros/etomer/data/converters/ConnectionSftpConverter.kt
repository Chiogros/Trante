package chiogros.etomer.data.converters

/*
class ConnectionSftpConverter {
    @TypeConverter
    fun dbToObj(con: ConnectionSftpDb): ConnectionSftpObj {
        return ConnectionSftpObj(host = con.connectionNetwork.host, user = con.connectionNetwork.user)
    }
    @TypeConverter
    fun objToDb(con: ConnectionSftpObj): ConnectionSftpDb {
        return ConnectionSftpDb(ConnectionNetwork(host = con.host, user = con.user, connection = Connection(con.name, con.enabled)))
    }
}*/