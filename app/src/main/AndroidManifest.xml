<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher${logo}"
        android:label="${app_name}"
        android:roundIcon="@mipmap/ic_launcher${logo}_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.SplashScreen"
        android:memtagMode="async"
        tools:targetApi="36">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
        TODO
        [grantUriPermissions]
        If you enable this feature, either by setting this attribute to "true" or by defining
        <grant-uri-permission> subelements, call Context.revokeUriPermission() when
        a covered URI is deleted from the provider.
        -->
        <provider
            android:name="${provider_name}"
            android:authorities="${package_name}${provider_name}"
            android:grantUriPermissions="true"
            android:exported="true"
            android:permission="android.permission.MANAGE_DOCUMENTS"
            android:icon="@mipmap/ic_launcher"
            android:label="${app_name}">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>
    </application>

</manifest>